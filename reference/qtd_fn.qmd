---
title: "Quarter-to-date execution function"
pagetitle: "Quarter-to-date execution function — qtd_fn"
---

::: {.qrtdown-source .dont-index}
Source: [`R/time_intelligence_fn.R`](https://codeberg.org/usrbinr/ti/src/branch/main/R/time_intelligence_fn.R)
:::

<p><code><a href="qtd_fn.qmd">qtd_fn()</a></code> is the function that is called by <code><a href="qtd.qmd">qtd()</a></code> when passed through to <a href="calculate.qmd">calculate</a>
</p>

## Usage

```r
qtd_fn(x)
```

## Arguments

<dl class="qrtdown-arguments">
<dt id="arg-x"><code>x</code><a class="anchor" aria-label="anchor" href="#arg-x"></a></dt>
<dd><p>ti object</p></dd>
</dl>

## Value

<p>dbi object
</p>

## Details

<p>This is internal non exported function that is nested in ti class and is called upon when the underlying function is called
by <a href="calculate.qmd">calculate</a>
This will return a dbi object that can converted to a tibble object with <code><a href="https://dplyr.tidyverse.org/reference/compute.html">dplyr::collect()</a></code>
</p>

## See Also

<p><code><a href="qtd.qmd">qtd()</a></code> for the function's intent
</p>

