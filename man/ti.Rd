% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{ti}
\alias{ti}
\title{Time-intelligence class}
\usage{
ti(
  datum = datum(),
  time_unit = time_unit(),
  value = value(),
  fn = fn(),
  action = action()
)
}
\arguments{
\item{datum}{A \link{datum} object holding the table and its date metadata.}

\item{time_unit}{A \link{time_unit} object giving the granularity.}

\item{value}{A \link{value} object naming the measure and output column.}

\item{fn}{An \link{fn} object holding the transformation to execute.}

\item{action}{An \link{action} object describing the calculation for
printing.}
}
\value{
A \code{ti} S7 object.
}
\description{
\code{ti} is the object returned by every time-intelligence function (\code{\link[=ytd]{ytd()}},
\code{\link[=yoy]{yoy()}}, \code{\link[=mtd]{mtd()}} and so on). It composes the data (\link{datum}), the
granularity (\link{time_unit}), the measure (\link{value}), the
calculation blueprint (\link{fn}) and the print narrative (\link{action})
into a single lazy object. Nothing is computed until it is passed to
\code{\link[=calculate]{calculate()}}.
}
\keyword{internal}
